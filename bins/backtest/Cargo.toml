[package]
name = "algo-bin-backtest"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[[bin]]
name = "algo-backtest"
path = "src/main.rs"

[dependencies]
algo-core = { path = "../../crates/core" }
algo-backtest = { path = "../../crates/backtest" }
algo-features = { path = "../../crates/features" }
algo-strategy = { path = "../../crates/strategy" }
algo-strategies = { path = "../../crates/strategies" }
algo-storage = { path = "../../crates/storage" }
algo-obs = { path = "../../crates/obs" }
clap = { workspace = true }
tracing = { workspace = true }
anyhow = { workspace = true }
