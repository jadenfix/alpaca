[package]
name = "algo-strategy"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
name = "algo_strategy"
path = "src/lib.rs"

[dependencies]
algo-core = { path = "../core" }
algo-features = { path = "../features" }
serde = { workspace = true }
smallvec = { workspace = true }
